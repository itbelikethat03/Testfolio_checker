__pycache__/
*.pyc
.ipynb_checkpoints/

# Local scratch/temp, if any ever lands in the repo by accident
*.tmp

# Obsidian per-machine UI state (the vault itself is versioned)
context/.obsidian/workspace*.json
context/.obsidian/cache
